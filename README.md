# user-management-crud-react
A CRUD (Create, Read, Update, Delete) application using React to manage users which uses a publicly available API like JSONPlaceholder (https://jsonplaceholder.typicode.com/) to fetch a list of users.
